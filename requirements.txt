# Demi v1 Dependencies
# Core dependencies for local LLM companion assistant

# LLM Integration
# (Ollama installed separately - see docs)

# Discord Integration (Phase 05)
discord.py>=2.6.0

# Telegram Integration
python-telegram-bot>=20.8

# HTTP client for LMStudio fallback
aiohttp>=3.8.0

# Environment configuration
python-dotenv>=1.0.0

# WebSocket support for dashboard and real-time features
websockets>=12.0

# Discord Voice Integration (Phase 08-03)
# Note: Requires FFmpeg and libopus system packages
# Ubuntu/Debian: sudo apt-get install ffmpeg libopus0
# macOS: brew install ffmpeg opus
# Windows: Download from https://ffmpeg.org/download.html
PyNaCl>=1.5.0  # Required for discord.py voice encryption
opuslib>=3.0.1  # Opus codec for Discord audio decode

# Android API (Phase 06)
fastapi>=0.115.0
uvicorn>=0.32.0
pydantic[email]>=2.9.0
passlib[bcrypt]>=1.7.4
PyJWT>=2.9.0
python-multipart>=0.0.12

# Database
SQLAlchemy>=2.0.0
alembic>=1.13.0

# Emotional System & Personality
# (No external dependencies - pure Python)

# Conductor & Monitoring
psutil>=5.9.0  # Resource monitoring
prometheus-client>=0.20.0  # Metrics (optional)

# Machine Learning (for predictive scaling)
scikit-learn>=1.3.0  # Optional - falls back gracefully if unavailable

# Logging
structlog>=24.1.0  # Optional - falls back to standard logging

# Voice I/O (Phase 08) - TTS
pyttsx3>=2.90  # Cross-platform TTS (fallback)
# piper-tts>=1.2.0  # High-quality neural TTS
# onnxruntime-gpu>=1.16.0; sys_platform == 'linux'  # GPU acceleration for Piper (Linux)
# onnxruntime>=1.16.0; sys_platform != 'linux'  # CPU fallback for other platforms
sounddevice>=0.4.6  # Audio playback for Piper TTS
g2p_en>=2.1.0  # Grapheme-to-Phoneme for lip sync

# Voice I/O (Phase 08)
faster-whisper>=1.0.0  # or openai-whisper
ctranslate2>=4.0  # Required for faster-whisper int8 quantization
# pyaudio>=0.2.11  # Commented out - requires PortAudio system libraries (optional for audio recording)
webrtcvad>=2.0.10  # Voice activity detection for Discord voice
numpy>=1.24.0

# Development
pytest>=7.4.0
pytest-asyncio>=0.21.0
pytest-cov>=4.1.0
black>=23.0.0
pyyaml>=6.0
